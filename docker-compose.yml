version: '3'

services:
  web:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db

  db:
    image: postgres
    environment:
      - POSTGRES_DB=bfd_backend_db
      - POSTGRES_USER=bfduser
      - POSTGRES_PASSWORD=bfdbackend2023
#    env_file:
#      - .env